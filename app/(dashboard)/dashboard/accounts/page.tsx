import AccountPage from "@/components/accounts/AccountPage";
import React from "react";

const page = async () => {
  return (
    <div>
      <AccountPage />
    </div>
  );
};

export default page;
